import numpy as np

# 8、数组的加减乘除
# (i, j, k, ...) + (a, b, c, ...) = (i + a, j + b, k + c, ...)
# (i, j, k, ...) - (a, b, c, ...) = (i - a, j - b, k - c, ...)
# (i, j, k, ...) * (a, b, c, ...) = (i * a, j * b, k * c, ...)
# (i, j, k, ...) / (a, b, c, ...) = (i / a, j / b, k / c, ...)

# 9、数组的加法和列表的加法的区别
# 列表的加法是拼接
a = [1, 2]
b = [1, 2]
print(a + b)
# 数组的加法不是拼接，见8